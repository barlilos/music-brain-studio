node_modules
out
release
pnpm-lock.yaml

# The real knowledge base is data, not source. Its formatting belongs to
# whatever writes it, and reformatting it here would produce a large diff
# that says nothing about the code.
data
